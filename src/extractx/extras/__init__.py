"""extras packages per docs/architecture.md §16.

optional integrations — `pydantic_ai` (default llm-backed selector and
interview capture, ADR-0002), `unstructured` (provisional default document
adapter, under research in docs/tasks/select-default-document-adapter.md),
`modal` / `ray` (remote executors, not part of the v1 end-user public
pact).
"""
