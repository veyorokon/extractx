"""llm-backed `Selector` implementations per docs/architecture.md §7 seam D.

the default llm-backed selector ships in `extras/pydantic_ai/` (see
ADR-0002). this package exists for alternative llm-backed selectors that
do not use pydantic-ai.
"""
