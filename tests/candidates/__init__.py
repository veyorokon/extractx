"""seam C phase 1 candidate-generation tests.

proof lane for the regex `CandidateStrategy` and the seam-C helper
surface (`candidate_id_for`, `build_candidate_set`,
`validate_source_span_against_view`). NER, clause, table, hybrid, and
C.alt generators land in their own focused test lanes.
"""
